apiVersion: v1
kind: Service
metadata:
  name: edusphere-service
spec:
  selector:
    app: edusphere
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30050
  type: NodePort
